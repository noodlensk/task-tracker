asyncapi: 2.4.0
info:
  title: Accounting
  version: 1.0.0
  description: TODO
channels:
  users-cud.created:
    publish:
      message:
        $ref: '../../../../api/asyncapi/users-cud/messages/user-created.yaml'
  users-cud.updated:
    publish:
      message:
        $ref: '../../../../api/asyncapi/users-cud/messages/user-updated.yaml'
  tasks-cud.created:
    publish:
      message:
        $ref: '../../../../api/asyncapi/tasks-cud/messages/task-created.yaml'
  tasks-cud.updated:
    publish:
      message:
        $ref: '../../../../api/asyncapi/tasks-cud/messages/task-updated.yaml'

